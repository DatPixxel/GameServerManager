--- game_server_manager.py
+++ game_server_manager_fixed.py
@@ -9,6 +9,7 @@
 import tkinter as tk

 from tkinter import messagebox, filedialog

 import subprocess

+import shlex

 import threading

 import os

 import sys

@@ -849,7 +850,7 @@
     "StarRupture": {

         "app_id": "3809400",

         "exe_path": "StarRuptureServerEOS.exe",

-        "default_params": "-Log -MULTIHOME=100.114.1.98 -port=7777 -QueryPort=27015",

+        "default_params": "-Log -port=7777 -QueryPort=27015",

         "default_ports": {"game": 7777, "query": 27015},

         "icon": "🌌",

         "config_path": "Saved/Config/WindowsServer",

@@ -2263,8 +2264,13 @@
         # Für andere Spiele: Standard-Parameter als Liste

         params = self.config.get("start_params", game_info.get("default_params", ""))

         if params:

-            # Parameter-String in Liste umwandeln (einfaches Split)

-            param_list = params.split()

+            # Parameter-String in Liste umwandeln (robust, unterstützt Quotes)

+            try:

+                param_list = shlex.split(params, posix=(os.name != 'nt'))

+            except ValueError as e:

+                # Fallback: simples Split (besser als kompletter Start-Abbruch)

+                self.log(f"⚠️ Konnte Startparameter nicht parsen ({e}). Nutze Fallback-Split.")

+                param_list = params.split()

             return [exe_path] + param_list

         return [exe_path]

     

@@ -7606,8 +7612,9 @@
                         process = subprocess.Popen(

                             cmd_list,

                             stdout=subprocess.PIPE,

-                            stderr=subprocess.PIPE,

+                            stderr=subprocess.STDOUT,

                             text=True,

+                            bufsize=1,

                             shell=False

                         )

                     else:

@@ -7615,8 +7622,9 @@
                         process = subprocess.Popen(

                             cmd_list,

                             stdout=subprocess.PIPE,

-                            stderr=subprocess.PIPE,

+                            stderr=subprocess.STDOUT,

                             text=True,

+                            bufsize=1,

                             shell=False,

                             creationflags=subprocess.CREATE_NO_WINDOW

                         )

@@ -7624,8 +7632,9 @@
                     process = subprocess.Popen(

                         cmd_list,

                         stdout=subprocess.PIPE,

-                        stderr=subprocess.PIPE,

+                        stderr=subprocess.STDOUT,

                         text=True,

+                        bufsize=1,

                         shell=False

                     )

                 

@@ -7649,16 +7658,31 @@
                 

                 # Zusätzliche Erfolgs-Prüfung: Existiert die Server-EXE?

                 game_info = SUPPORTED_GAMES.get(server_config["game"], {})

-                exe_name = game_info.get("exe", "")

-                if exe_name:

-                    # Suche nach der EXE im Server-Verzeichnis

-                    for root, dirs, files in os.walk(server_dir):

-                        if exe_name in files:

-                            install_success = True

-                            break

-                

+                exe_rel = game_info.get("exe_path", "")

+                exe_found = False

+                expected_exe = ""

+                if exe_rel:

+                    expected_exe = os.path.join(server_dir, exe_rel.replace("/", os.sep))

+                    if os.path.exists(expected_exe):

+                        exe_found = True

+                    else:

+                        # Fallback: falls exe_path je nach Build abweicht, suche nach dem Dateinamen

+                        exe_base = os.path.basename(exe_rel)

+                        for root, dirs, files in os.walk(server_dir):

+                            if exe_base in files:

+                                exe_found = True

+                                expected_exe = os.path.join(root, exe_base)

+                                break

+                    if instance:

+                        if exe_found:

+                            instance.log(f"✅ EXE gefunden: {expected_exe}")

+                        else:

+                            instance.log(f"⚠️ EXE nicht gefunden (erwartet): {os.path.join(server_dir, exe_rel.replace('/', os.sep))}")

+                    if exe_found:

+                        install_success = True

+

                 # Erfolg: Entweder returncode 0 ODER Success-Meldung ODER EXE gefunden

-                if process.returncode == 0 or install_success:

+                if process.returncode == 0 and install_success:

                     self.config_manager.servers[server_id]["installed"] = True

                     self.config_manager.save_servers()

                     if instance:
